<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingTop="8dp"
    android:paddingRight="16dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/image_item_user_found_avatar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/text_item_user_found_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="8dp"
        android:text="Lam nguyen"
        android:textColor="#000"
        android:textSize="@dimen/h1"
        app:layout_constraintBottom_toBottomOf="@+id/image_item_user_found_avatar"
        app:layout_constraintStart_toEndOf="@+id/image_item_user_found_avatar"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/image_message_to_friend"
        android:layout_width="@dimen/icon_member_list_size"
        android:layout_height="@dimen/icon_member_list_size"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/image_call_to_friend"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_message_black_24dp" />

    <ImageView
        android:id="@+id/image_call_to_friend"
        android:layout_width="@dimen/icon_member_list_size"
        android:layout_height="@dimen/icon_member_list_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_telephone" />
</androidx.constraintlayout.widget.ConstraintLayout>